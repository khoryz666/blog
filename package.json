{
  "name": "blog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.17.1"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check",
    "lint": "oxlint",
    "preview": "astro preview",
    "verify:sidebar": "npm run build && node scripts/verify-sidebar.mjs",
    "test": "npm run check && npm run lint && npm run verify:sidebar"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.16.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "oxlint": "^1.75.0",
    "playwright-core": "1.62.1",
    "typescript": "~6.0.2"
  }
}
